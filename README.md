# Discordpy Template

---

## Features

- `!ping` — Shows bot latency in milliseconds (ms).
- `!hello` — Greets the user.

---

## Requirements

- Python 3.8+
- `discord.py` library

Install with:

```bash
pip install discord.py
